{
  "name": "vybify",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/prefetch-stars.mjs && astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "@astrojs/mdx": "^4.3.14",
    "@tailwindcss/vite": "^4.0.0",
    "astro": "^5.1.1",
    "fuse.js": "^7.3.0",
    "tailwindcss": "^4.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
